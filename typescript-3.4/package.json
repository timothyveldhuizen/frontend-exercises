{
  "name": "typescript-3.4",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "rm -r dist",
    "lint": "npx tslint -p . -c tslint.json",
    "build": "npx tsc"
  },
  "dependencies": {},
  "devDependencies": {
    "tslint": "^5.16.0",
    "typescript": "^3.4.5"
  }
}
